import 'package:dev_login/Screens/WebsiteNo/Components/body.dart';
import 'package:flutter/material.dart';

class WebsiteNo extends StatelessWidget {
  const WebsiteNo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}